import React from 'react';
import {View, Text, Image, Dimension, ImageBackground} from 'react-native';
import PropTypes from 'prop-types';

const Login = props => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
};

// Login.propTypes = {

// }

export default Login;
